/**
 * @export
 * @param {number[]} bytes
 * @returns {number[]}
 */
export function decodeInstruction(bytes) {
    const size = bytes.length;
    switch(size) {
        default:
            break;
        case 1:
            break;
        case 2:
            break;
        case 3:
            break;
        case 4:
            break;
    }
    return [];
}